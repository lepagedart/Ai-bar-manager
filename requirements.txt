# --- Web App & Core Utilities ---
flask
flask-session
python-dotenv
reportlab
requests
email-validator
openai == 0.28.0

# --- LangChain RAG & PDF Processing ---
langchain>=0.1.0
langchain-community==0.3.24
langchain-core==0.3.62
langchain-text-splitters==0.3.8
pypdf
faiss-cpu==1.11.0

# --- HuggingFace Local Embedding ---
sentence-transformers==2.2.2

# --- Optional (used earlier, keep if still relevant) ---
tiktoken  # Only needed if still using OpenAI